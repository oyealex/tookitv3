---
name: "OPSX: 验证"
description: "在归档前验证实现是否匹配变更制品"
category: Workflow
tags: [workflow, verify, experimental]
---

验证实现是否匹配变更制品（规范、任务、设计）。

**输入**：可选择在 `/opsx:verify` 后指定变更名称（例如 `/opsx:verify add-auth`）。如果省略，检查是否可以从对话上下文中推断。如果模糊或不确定，你必须提示选择可用变更。

**步骤**

1. **如果未提供变更名称，提示选择**

   运行 `openspec list --json` 获取可用变更。使用 **AskUserQuestion 工具** 让用户选择。

   显示具有实现任务（存在 tasks 制品）的变更。
   如果可用，包含每个变更使用的模式。
   将具有未完成任务的变更标记为"（进行中）"。

   **重要**：不要猜测或自动选择变更。始终让用户选择。

2. **检查状态以了解模式**
   ```bash
   openspec status --change "<name>" --json
   ```
   解析 JSON 以了解：
   - `schemaName`：使用的工作流程（例如 "spec-driven"）
   - 此变更存在哪些制品

3. **获取变更目录并加载制品**

   ```bash
   openspec instructions apply --change "<name>" --json
   ```

   这返回变更目录和上下文文件。从 `contextFiles` 读取所有可用制品。

4. **初始化验证报告结构**

   创建具有三个维度的报告结构：
   - **完整性**：跟踪任务和规范覆盖率
   - **正确性**：跟踪需求实现和场景覆盖率
   - **一致性**：跟踪设计遵守和模式一致性

   每个维度可以有 CRITICAL、WARNING 或 SUGGESTION 问题。

5. **验证完整性**

   **任务完成**：
   - 如果 contextFiles 中存在 tasks.md，读取它
   - 解析复选框：`- [ ]`（未完成）vs `- [x]`（完成）
   - 计算完成 vs 总任务
   - 如果存在未完成任务：
     - 为每个未完成任务添加 CRITICAL 问题
     - 建议："完成任务：<描述>"或"如果已实现则标记为完成"

   **规范覆盖率**：
   - 如果 `openspec/changes/<name>/specs/` 中存在增量规范：
     - 提取所有需求（用"### Requirement:"标记）
     - 对于每个需求：
       - 搜索代码库中与需求相关的关键字
       - 评估实现是否存在
     - 如果需求看起来未实现：
       - 添加 CRITICAL 问题："未找到需求：<需求名称>"
       - 建议："实现需求 X：<描述>"

6. **验证正确性**

   **需求实现映射**：
   - 对于增量规范中的每个需求：
     - 搜索代码库中的实现证据
     - 如果找到，记下文件路径和行范围
     - 评估实现是否符合需求意图
     - 如果检测到分歧：
       - 添加 WARNING："实现可能与规范不符：<详情>"
       - 建议："Review <file>:<lines> vs需求 X"

   **场景覆盖率**：
   - 对于增量规范中的每个场景（用"#### Scenario:"标记）：
     - 检查代码中是否处理了条件
     - 检查是否存在覆盖该场景的测试
     - 如果场景看起来未覆盖：
       - 添加 WARNING："场景未覆盖：<场景名称>"
       - 建议："为场景添加测试或实现：<描述>"

7. **验证一致性**

   **设计遵守**：
   - 如果 contextFiles 中存在 design.md：
     - 提取关键决策（查找如"Decision:"、"Approach:"、"Architecture:"等部分）
     - 验证实现是否遵循这些决策
     - 如果检测到矛盾：
       - 添加 WARNING："未遵守设计决定：<决定>"
       - 建议："更新实现或修改 design.md 以匹配现实"
     - 如果没有 design.md：跳过设计遵守检查，注明"No design.md 可验证"

   **代码模式一致性**：
     - 审查新代码是否与项目模式一致
     - 检查文件命名、目录结构、编码风格
     - 如果发现重大偏差：
       - 添加 SUGGESTION："代码模式偏差：<详情>"
       - 建议："考虑遵循项目模式：<示例>"

8. **生成验证报告**

   **摘要记分卡**：
   ```
   ## 验证报告：<change-name>

   ### 摘要
   | 维度          | 状态             |
   |--------------|------------------|
   | 完整性        | X/Y 任务，N 需求  |
   | 正确性        | M/N 需求已覆盖    |
   | 一致性        | 已遵守/有问题     |
   ```

   **按优先级的问题**：

   1. **CRITICAL**（归档前必须修复）：
      - 未完成的任务
      - 缺失的需求实现
      - 每个都有具体、可操作的建议

   2. **WARNING**（应该修复）：
      - 规范/设计分歧
      - 缺失的场景覆盖率
      - 每个都有具体建议

   3. **SUGGESTION**（值得修复）：
      - 模式不一致
      - 小的改进
      - 每个都有具体建议

   **最终评估**：
   - 如果有 CRITICAL 问题："发现 X 个关键问题。修复后再归档。"
   - 如果只有警告："没有关键问题。Y 个警告要考虑。可以归档（带注明的改进）。"
   - 如果全部通过："所有检查通过。可以归档。"

**验证启发式**

- **完整性**：专注于客观的检查表项目（复选框、需求列表）
- **正确性**：使用关键字搜索、文件路径分析、合理推断——不需要完美确定
- **一致性**：寻找明显的不一致，不要挑剔风格
- **误报**：不确定时，优先 SUGGESTION over WARNING，WARNING over CRITICAL
- **可操作性**：每个问题必须有具体建议，在适当时提供文件/行引用

**优雅降级**

- 如果只存在 tasks.md：只验证任务完成，跳过规范/设计检查
- 如果存在 tasks + specs：验证完整性和正确性，跳过设计
- 如果是完整制品：验证所有三个维度
- 始终注明跳过了哪些检查以及为什么

**输出格式**

使用清晰的 markdown：
- 表格用于摘要记分卡
- 分组列表用于问题（CRITICAL/WARNING/SUGGESTION）
- 代码引用格式：`file.ts:123`
- 具体、可操作的建议
- 不要有像"考虑审查"这样的模糊建议
